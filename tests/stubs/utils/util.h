/**
 * @file utils/util.h (host test stub)
 * @brief Empty shim for NavHAL's utils/util.h. kernel/utils.c includes it
 *        but the host tests don't call hal_memcpy/hal_strlen/etc., so a
 *        bare include guard suffices.
 */
#ifndef VAIOS_TEST_STUB_UTILS_UTIL_H
#define VAIOS_TEST_STUB_UTILS_UTIL_H
#endif
